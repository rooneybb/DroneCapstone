package threads;

import java.io.*;
import java.net.*;

import config.Config;

public class WLWorker extends Thread{
	/*
	Socket socket; 
	Config c = Config.getInstance();
	WLWorker(Socket s) { socket = s;}
	
	public void run() {
		try {
			WLHelper wl = new WLHelper();
			wl.getTickets();
		} finally { 
			try {
				if (!socket.isClosed()) 
					socket.close();
			} catch (IOException e) { e.printStackTrace(); } 	
		}
	}*/
}
